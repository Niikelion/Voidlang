module functions

void newline() -> print("\n")

T add<T>(T a, T b) -> a + b //NOT IMPLEMENTED YET

void main()
{
    print(x)
    newline()
    print(y)
}

int min(int a, int b)
{
    ret b < a ? b : a
}

int max(int a, int b) -> b > a ? b : a

int min<T>(T a, T b)        //NOT IMPLEMENTED YET
{
    return b < a ? b : a
}